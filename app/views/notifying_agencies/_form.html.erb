<%= form_for(@notifying_agency) do |f| %>
  <% if @notifying_agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notifying_agency.errors.count, "error") %> prohibited this notifying_agency from being saved:</h2>

      <ul>
      <% @notifying_agency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
